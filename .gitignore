# =============================================================================
# 敏感信息守则：真实配置、密钥、运行产物一律不入库
# 提交前建议跑一次：python scripts/check_secrets.py
# =============================================================================

# ---------------------------------------------------------------------------
# 1) 配置与密钥（最重要）
# 规则是「先全部忽略 config*.json，再把示例配置放回来」，这样以后不管叫
# config.private.json / config.local.json / config.prod.json 都不会被误提交。
# ---------------------------------------------------------------------------
config*.json
!config.example.json
*.env
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.*
token.txt
*.token
auth.json
service-account*.json

# 配置写回时自动产生的备份
*.bak
config.json.bak

# ---------------------------------------------------------------------------
# 2) 运行产物（含本机路径、下载记录、检索关键词）
# ---------------------------------------------------------------------------
paperdown/
literature_runs/

# ---------------------------------------------------------------------------
# 3) 本地临时与日志
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.log
*.tmp
.tmp_*/
.tmp_llm_tests/
.tmp_llm_probe*/
.tmp_new_sources/
.tmp_key_precedence/

# ---------------------------------------------------------------------------
# 4) 编辑器 / 系统文件（常混入本机路径或凭据）
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# 5) 输出与压缩包
# ---------------------------------------------------------------------------
*.zip
exports/
